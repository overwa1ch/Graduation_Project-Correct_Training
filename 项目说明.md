# 项目进度同步

## 一、项目迁移进度总结

### 🎯 当前目标
将开源 **AI Workout Assistant (Web)** 迁移为 Flutter 端的新产品形态，实现：
- Hybrid 推理；
- 支持「先录制后评估」；
- 以 Dart 重写规则引擎；
- 兼容 **MoveNet17** 与 **BlazePose33** 骨架模型；
- 结合离线批处理与云端增强。

目标：提升评估准确性与兼容性。

---

### ✅ 已完成工作
- **规则规范说明书 v0.1**：定义字段、必填项与取值范围；
- **关键点语义映射表**：统一 MoveNet17 与 BlazePose33 的点位含义；
- **Squat 动作规则模板**（`squat.v1.json`）完成；
- **字段校验清单**：定义所有字段的校验逻辑；
- **离线分析脚本设计**：从视频 / kp.json → 结果输出流程。

---

### ⚙️ 进行中的工作
- 编写离线分析脚本，实现「视频→关键点→计数与评分」闭环；
- 搭建批处理流程，验证 `squat.v1` 模板可在 Flutter 端运行。

---

### 🚀 下阶段计划（里程碑 A 收尾）
1. **完善深蹲计数与相位检测**：宽松/严苛两档阈值；
2. **实现 RuleSet.fromJson()**：规则 JSON → Dart 对象；
3. **产出测试样例**：两段视频及关键点 JSON，输出计数与角度曲线。

---

### ⚠️ 风险与关注点
- **模型适配差异**：17点与33点关键点定义不一致；
- **性能与发热**：需评估采样率与分辨率；
- **规则扩展性**：保持模板统一以支持多动作扩展。

---

### 🤝 团队协作需求
- 统一样例数据格式与分辨率；
- 规则配置建议使用 **JSON**；
- 确认云端增强接口上线时间。

---

## 二、改造方案概要

### 1️⃣ 规则引擎：JS → Dart
- 目标：迁移 Web 端规则为 Dart 可解析格式；
- 模型适配：定义 `modelAdapters`；
- 配置格式：JSON + 字段校验；
- 字段包括：`angles[]`, `phases[]`, `counts`, `scoreWeights`, `evidence`, `cues`, `strictness`；
- 每个动作支持 **strict / relaxed** 两套参数。

---

### 2️⃣ 推理引擎抽象（PoseEngine）与 Keypoint Adapter
- 接口：`init()`, `infer(frame)`, `close()`；
- 实现：
  - `MLKitEngine`（BlazePose 33 点）
  - `MoveNetEngine`（TFLite 17 点）
- Keypoint Adapter：统一输出关键点；
- 默认引擎：**ML Kit**，保留 MoveNet 开关。

---

### 3️⃣ 「先录后评」离线分析管线
- **流程**：
  1. 视频录制或导入 → 720p@30fps；
  2. 批处理推理（可抽样）；
  3. 滤波与平滑；
  4. 相位分割；
  5. 计数与评分；
  6. 证据帧选择；
  7. 汇总结果。
- **结果展示**：
  - 总分 + 分项评分；
  - 骨架叠加回放；
  - 角度曲线；
  - 建议清单；
- **导出格式**：JSON / CSV。

---

### 4️⃣ Hybrid 模式：困难样本触发 + 云端增强
- 判断依据：关键点覆盖率、遮挡率、抖动度；
- 隐私选项：
  - A：仅上传关键点；
  - B：上传视频 + 关键点（需确认）；
- 云端返回校正指标并合并显示“已增强”。

---

### 5️⃣ 角度 / 阶段计算内核
- 平滑算法：**One Euro / Savitzky–Golay**；
- 自动剔除低置信度点；
- 角度库：髋、膝、踝、肘、躯干；
- 相位机：导数符号 + 峰谷检测 + 持续时间判断。

---

### 6️⃣ 动作库落地
- 第一批动作：
  - 深蹲（Squat）
  - 俯卧撑（Push-up）
- 每个动作提供 strict / relaxed；
- 扩展动作：Lunge、RDL、Plank；
- 模板版本化：`squat.v1` 等。

---

### 7️⃣ UI/UX 设计
- 风格：黑白灰极简主义；
- 强调色：
  - 红色：错误；
  - 浅绿色：完成；
- 结果界面：
  - 骨架叠加播放器；
  - 分项评分卡；
  - 单一角度曲线；
  - 主/次建议；
- 设置项：
  - 阈值档（宽松/严苛）；
  - 引擎选择（MLKit / MoveNet）；
  - 数据备份策略。

---

### 8️⃣ 端上性能与电量
- 动态采样率：2× 或 3×；
- 分辨率回退策略：720p → 540p；
- 加速方案：
  - iOS：Metal；
  - Android：NNAPI / GPU Delegate；
- 性能目标：
  - 批处理总时长 ≤ 视频时长的 0.5×；
  - 低端设备 ≤ 1×。

---

### 9️⃣ 指标等价定义（17 点 vs 33 点）
- 提供双公式定义；
- 示例：躯干前倾 = `angle(shoulder–hip, vertical)`；
- 降级策略：
  - 若模型缺失点位，则使用近似指标；
  - 动态调整评分权重。

---

### 🔟 数据、合规与清理
- 本地存储：视频 + 关键点；
- 自动清理：按天数 / 空间；
- 云端隐私：
  - 默认匿名；
  - 上传视频需二次确认；
- 日志：仅记录性能数据；
- 核心数据结构：
  - `Session`：评分、问题、引擎信息；
  - `KeypointSeries`：时间戳、点坐标、置信度；
  - `RuleSet`：模型适配、阶段划分、评分权重等。

---

## 三、优化后的执行方案（6 周）

| 周次 / 里程碑 | 关键任务 | 交付物 |
|----------------|----------|--------|
| **第 1–1.5 周（A）** | 规则 JSON→Dart 解析；完成 Squat 模板；离线批处理管线 | 深蹲视频输入 → JSON 结果 |
| **第 1–2 周（B）** | 定义 PoseEngine；集成 ML Kit；构建 Keypoint Adapter | 统一关键点 JSON 输出 |
| **第 2.5–3 周** | 性能 Smoke Test（运行时/发热/丢帧） | 性能报告与推荐参数 |
| **第 3–4 周（C）** | 实现困难样本检测 + 云端增强提示 + 证据帧选择 | 遮挡视频展示云端分析建议 |
| **第 4–5 周（D）** | 扩展 Push-up 模板；完善评分维度；实现自动清理 | 输出俯卧撑计数与评分结果 |
| **第 6 周（收尾）** | 全流程走查与演示；可选接入真实云端增强 | 完整体验与性能报告 |

---

## 四、小结
本阶段目标：
> 打造端侧可配置、可插拔的运动动作评测框架。  
> 在 Flutter 平台复用 Web 端规则体系，通过多模型适配、离线分析与混合推理，实现隐私与准确性的平衡。

后续方向：
- 扩展更多动作；
- 持续优化性能、用户体验与数据合规性。
