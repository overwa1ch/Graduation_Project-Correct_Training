# Hybrid 流程端到端测试计划

## 样本集
- 正常样本 ×2（无触发）
- 遮挡/低光 ×2（触发）
- 节奏异常 ×1（触发 + 合并）

## 断言
- 触发与否、reasons 合理性；
- `cloud_payload.json` 存在且体积合规；
- `result.json.hybrid` 字段完整；
- 差异较大样本触发回退；
- 性能与日志无异常。

## 验收
- 所有校验通过，结果契约一致，性能预算内。
