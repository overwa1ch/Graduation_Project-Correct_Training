name: aiwa_cli
description: AIWA offline analyzer CLI (pure Dart)
version: 0.1.0
environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  args: ^2.6.0
  path: ^1.9.0
  aiwa_core:
    path: ../aiwa_core

dev_dependencies:
  lints: ^5.0.0
