# squat_long 样本

- **类别**：性能样本
- **目的**：验证超长素材 (> 5 min) 的耗时、内存与降级策略。
- **输入**：`input.mp4`（6 分钟，30 fps）。
- **预期现象**：
  - 长时素材触发批次分段，整体 `pipelineRtf` ≈ 0.97。
  - `perf.json` 中 `memPeakMB` 控制在 540 以内。
  - 结果保持稳定，计数 168 次，`coverage` 0.92。
