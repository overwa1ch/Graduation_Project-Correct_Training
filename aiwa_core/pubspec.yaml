name: aiwa_core
description: Core logic for AIWA (pure Dart)
version: 0.1.0
environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  csv: ^6.0.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
